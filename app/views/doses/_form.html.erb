<%= simple_form_for [@cocktail, @new_dose] do |f| %>
<%= f.input :description, label: 'description de la dose' %>
<%= f.input :ingredient_id, collection: @ingredients %>
<%= f.button :submit, class:'btn btn-info' %>
<% end %>
